{
  "name": "wiremock-mock-server",
  "version": "1.0.0",
  "description": "mock server with wiremock implementation",
  "main": "index.js",
  "scripts": {
    "start:dw1": "wiremock --root-dir ./dw1-mocks --port 8100",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alex.galvis.sistemas@gmail.com",
  "license": "ISC",
  "dependencies": {
    "wiremock": "^2.33.2"
  }
}
